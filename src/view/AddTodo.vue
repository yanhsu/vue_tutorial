<script setup>
    import { ref, defineEmits } from 'vue';
    // 定義傳給parent之事件名稱(defineEmit)

    // 宣告輸入待辦綁定的變數(ref)
    
    //發送新增待辦事件至parent component(function)

</script>
<template>
    <div class="center">
        <!-- 輸入待辦按下按鈕後新增(使用input與button) -->
    </div>
</template>
<style scoped>
.center {
    display: flex;
    justify-content: center;
    margin-top: 3em;
}
</style>